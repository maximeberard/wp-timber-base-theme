{%- macro svg(classes, icon, id) -%}
    {% spaceless %}
        <svg class="{{ classes|join(' ') }} {{ icon }}" {% if id %} id="{{ id }}" {% endif %}>
            <use xlink:href="#{{ icon }}"></use>
        </svg>
    {% endspaceless %}
{%- endmacro -%}
